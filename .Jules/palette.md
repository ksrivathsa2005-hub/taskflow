## 2026-03-13 - [ReviewModal Accessibility]
**Learning:** Modal components require specific ARIA roles (`dialog`, `aria-modal`) and explicit associations between labels and form controls to be fully accessible. Star rating buttons benefit from `aria-label` and `aria-pressed` to communicate their state.
**Action:** Always include ARIA attributes when creating or modifying modal components and complex form controls like star ratings.
