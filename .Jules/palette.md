## 2026-04-24 - [Accessibility/UX Improvements in Navbar]
**Learning:** Interactive elements implemented as `div` with click handlers are not keyboard-accessible by default and lack standard focus indicators.
**Action:** Always use semantic `<button>` or `<a>` tags for clickable elements and ensure they have descriptive `aria-label` and visible focus states.
