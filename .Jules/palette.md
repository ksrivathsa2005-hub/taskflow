## 2025-05-14 - Consistency in Review UI
**Learning:** Using native browser `prompt()` for user input creates a jarring and inconsistent UX when a custom, styled modal exists for the same purpose elsewhere in the application.
**Action:** Always prefer custom UI components (like `ReviewModalComponent`) over native dialogs to maintain brand consistency and provide a better interactive experience.
