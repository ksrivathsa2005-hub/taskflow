## 2026-05-24 - Improving Navbar Accessibility and Interaction
**Learning:** Non-semantic elements with click handlers are invisible to keyboard users and screen readers unless explicitly given roles and tabIndex. Using semantic `<button>` elements with `focus-visible` rings and ARIA labels provides a much more robust and inclusive experience.
**Action:** Always use semantic elements for interactive components and ensure `focus-visible` states are clearly styled.
